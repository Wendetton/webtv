export default function Carousel() {
  // Depois você faz o upload e lista das imagens no painel admin
  return (
    <div style={{
      width: "100%",
      height: "100%",
      background: "#eee",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      color: "#888",
      fontSize: 32
    }}>
      Carrossel de imagens aqui
    </div>
  );
}
